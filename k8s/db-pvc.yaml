apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mempalace-db-pvc
  namespace: mempalace
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # storageClassName: standard   # uncomment and adjust for your cluster
